# BE/.env — local overrides used when running the backend from the BE working directory
# The project uses `spring.config.import: optional:file:.env` in application.yml
# When you run the app from the IDE the working directory is usually BE/ — put overrides here.

# For the dev profile the code composes the JDBC url like:
# jdbc:postgresql://${DATABASE_DEV_URL:db:5432/alfri}
DATABASE_DEV_URL=localhost:5432/alfri
DATABASE_DEV_USER=alfri
DATABASE_DEV_PASSWORD=changeme

# Production-formatted JDBC URL (used if prod profile or application.yml references it)
DATABASE_PROD_URL=jdbc:postgresql://localhost:5432/alfri
DATABASE_PROD_USER=alfri
DATABASE_PROD_PASSWORD=changeme

# Helpful defaults for local runs
X_API_KEY=local-dev-key
PYTHON_SERVICE_URL=http://localhost:5000


